# 🚀 GitHub Actions 自動部署設定指南

## ✅ 無需額外設定！

本專案使用 `gh-pages` npm 套件進行部署，**不需要修改 GitHub 設定**。

推送到 main 分支後會自動：
1. 建置專案
2. 部署到 `gh-pages` 分支
3. GitHub Pages 自動從 `gh-pages` 分支發布

---

### 步驟 1：確認 GitHub Pages 設定

前往：
```
https://github.com/yanchen184/dtmrs-website/settings/pages
```

確認設定為：
- **Source**: Deploy from a branch
- **Branch**: gh-pages / (root)

（這應該已經是預設設定，無需修改）

---

### 步驟 2：等待自動部署

推送程式碼到 main 分支後：

1. **查看 Actions 執行狀態**
   ```
   https://github.com/yanchen184/dtmrs-website/actions
   ```

2. **部署時間**
   - 建置時間：約 1-2 分鐘
   - 部署時間：約 1-2 分鐘
   - 總計：約 3-5 分鐘

3. **確認部署成功**
   - 綠色勾勾 ✅ = 成功
   - 紅色叉叉 ❌ = 失敗（點擊查看錯誤）

---

### 步驟 3：驗證網站

部署完成後訪問：
```
https://yanchen184.github.io/dtmrs-website/
```

按 `F12` 打開開發者工具，在 Console 應該會看到：
```
DTMRS Website v2.8.0 - 修復 GitHub Pages 影片路徑問題
```

---

## 🔧 自動部署工作流程

### 觸發條件
- 推送到 `main` 分支時自動觸發
- 也可以手動觸發（在 Actions 頁面）

### 部署步驟
```
1. 📥 Checkout 程式碼
2. 🔧 設置 Node.js 20
3. 📦 安裝依賴 (npm ci)
4. 🏗️ 建置專案 (npm run build)
5. 📤 上傳建置結果到 GitHub Pages
6. 🚀 部署到線上環境
```

---

## 💡 常見問題

### Q1: 推送後沒有自動部署？
**A:** 檢查 GitHub Pages 設定是否改為 "GitHub Actions"

### Q2: Actions 失敗了怎麼辦？
**A:**
1. 前往 Actions 頁面查看錯誤訊息
2. 點擊失敗的 workflow 查看詳細 log
3. 常見問題：
   - Node.js 版本問題 → 已設定為 Node.js 20
   - 依賴安裝失敗 → 檢查 package.json
   - 建置失敗 → 檢查 TypeScript 錯誤

### Q3: 部署後影片還是沒有顯示？
**A:**
1. **清除瀏覽器快取**：按 `Ctrl + Shift + R` 強制重新載入
2. **檢查 Console**：確認版本號是否為 v2.8.0
3. **檢查影片路徑**：所有影片應該使用 `/dtmrs-website/assets/videos/...`
4. **等待 CDN 更新**：GitHub Pages CDN 可能需要 5-10 分鐘更新

### Q4: 可以同時使用手動部署嗎？
**A:** 可以！`npm run deploy` 仍然可以使用，但建議使用自動部署

---

## 📊 監控部署狀態

### 方法 1：GitHub Actions Badge
在 README.md 中添加徽章：
```markdown
[![Deploy to GitHub Pages](https://github.com/yanchen184/dtmrs-website/actions/workflows/deploy.yml/badge.svg)](https://github.com/yanchen184/dtmrs-website/actions/workflows/deploy.yml)
```

### 方法 2：Email 通知
GitHub 會在部署失敗時自動發送 Email 通知

### 方法 3：Slack/Discord 整合
可以在 workflow 中添加通知步驟

---

## 🎯 下次推送時

只需要：
```bash
git add .
git commit -m "your message"
git push origin main
```

GitHub Actions 會自動：
1. ✅ 建置專案
2. ✅ 執行測試（如果有配置）
3. ✅ 部署到 GitHub Pages
4. ✅ 更新線上網站

完全自動化，無需手動操作！
